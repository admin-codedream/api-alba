<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.api.alba.mapper.owner.WorkplaceMapper">

    <resultMap id="workplaceMap" type="com.api.alba.domain.owner.Workplace">
        <id property="id" column="ID"/>
        <result property="ownerId" column="OWNER_ID"/>
        <result property="name" column="NAME"/>
        <result property="address" column="ADDRESS"/>
        <result property="inviteCode" column="INVITE_CODE"/>
        <result property="latitude" column="LATITUDE"/>
        <result property="longitude" column="LONGITUDE"/>
        <result property="allowedRadiusMeters" column="ALLOWED_RADIUS_METERS"/>
        <result property="useLocationRestriction" column="USE_LOCATION_RESTRICTION"/>
        <result property="createdAt" column="CREATED_AT"/>
    </resultMap>

    <insert id="insert" parameterType="com.api.alba.domain.owner.Workplace" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO WORKPLACES (OWNER_ID, NAME, ADDRESS, INVITE_CODE, LATITUDE, LONGITUDE, ALLOWED_RADIUS_METERS, USE_LOCATION_RESTRICTION)
        VALUES (#{ownerId}, #{name}, #{address}, #{inviteCode}, #{latitude}, #{longitude}, #{allowedRadiusMeters}, #{useLocationRestriction})
    </insert>

    <select id="findById" resultMap="workplaceMap">
        SELECT ID, OWNER_ID, NAME, ADDRESS, INVITE_CODE, LATITUDE, LONGITUDE, ALLOWED_RADIUS_METERS, USE_LOCATION_RESTRICTION, CREATED_AT
        FROM WORKPLACES
        WHERE ID = #{id}
    </select>

    <select id="findByInviteCode" resultMap="workplaceMap">
        SELECT ID, OWNER_ID, NAME, ADDRESS, INVITE_CODE, LATITUDE, LONGITUDE, ALLOWED_RADIUS_METERS, USE_LOCATION_RESTRICTION, CREATED_AT
        FROM WORKPLACES
        WHERE INVITE_CODE = #{inviteCode}
    </select>

</mapper>
