<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 공통 로그 패턴 -->
    <property name="CONSOLE_LOG_PATTERN" value="%boldMagenta(%d{YYYY-MM-dd HH:mm:ss}) [%thread] [%boldYellow(%-5level)] %cyan(%logger{5}.%M) - %msg %n" />
    <property name="FILE_LOG_PATTERN" value="%d{YYYY-MM-dd HH:mm:ss.SSS} [%thread] %5level %logger - %msg%n" />

    <!-- 로그 파일명 -->
    <springProperty name="FILENAME" source="logging.filename" defaultValue="albam" />
    <!-- 로그 파일 저장 위치 -->
    <springProperty name="LOG_PATH" source="logs.path" defaultValue="./logs"/>
    <!-- Slack Webhook URL -->
    <springProperty name="SLACK_WEBHOOK_URL" source="slack.webhook-url" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Slack Appender (ERROR 레벨만 전송) -->
    <appender name="SLACK" class="com.github.maricn.logback.SlackAppender">
        <webhookUri>${SLACK_WEBHOOK_URL}</webhookUri>
        <layout class="ch.qos.logback.classic.PatternLayout">
            <pattern>*[%level]* %d{yyyy-MM-dd HH:mm:ss} | %logger{30}.%M | %msg%n%ex{short}</pattern>
        </layout>
        <username>Alba API</username>
        <iconEmoji>:rotating_light:</iconEmoji>
        <colorCoding>true</colorCoding>
    </appender>

    <appender name="ASYNC_SLACK" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="SLACK" />
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <springProfile name="dev">
        <logger name="jdbc.resultsettable" level="WARN"/>

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>
    <springProfile name="!dev">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/${FILENAME}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/${FILENAME}.%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>7</maxHistory>
            </rollingPolicy>
            <encoder>
                <pattern>${FILE_LOG_PATTERN}</pattern>
                <immediateFlush>true</immediateFlush>
            </encoder>
        </appender>

        <logger name="jdbc.resultsettable" level="WARN"/>

        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE" />
            <appender-ref ref="ASYNC_SLACK" />
        </root>
    </springProfile>

    <!-- 스케줄러 로그 중복 방지 -->
    <springProfile name="dev">
        <logger name="com.api.alba.scheduler" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE" />
        </logger>
    </springProfile>
    <springProfile name="!dev">
        <logger name="com.api.alba.scheduler" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="FILE" />
        </logger>
    </springProfile>

</configuration>